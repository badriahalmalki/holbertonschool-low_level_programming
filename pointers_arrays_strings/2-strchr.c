#include "main.h"
#include <stddef.h>
/**
 * _strchr - 
 * @c: the character to locate
 * @s: the string
 * retern: a pointer or null
 */
char *_strchr(char *s, char c)
{
unsigned int i;
while (*s != '\0')
{
if ((unsigned char)*s == s)
{return (char *)s;
S++;
if (s == '\0')
{return (cHAR *)S;
}
return NULL;
}
